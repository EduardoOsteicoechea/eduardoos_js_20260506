name: Build and Deploy Vite App to EC2

on:
  push:
    branches:
      - main 

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' 

      - name: Install Dependencies
        run: npm ci

      - name: Build Vite App (includes JSON-LD postbuild validation)
        run: npm run build
        env:
          PUBLIC_BACKEND_API_BASE_URL: "https://eduardoos.com"

      - name: Deploy via SSH and Rsync
        uses: easingthemes/ssh-deploy@main
        env:
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
          REMOTE_HOST: ${{ secrets.EC2_HOST }}
          REMOTE_USER: ${{ secrets.EC2_USERNAME }}
          SOURCE: "build/" 
          TARGET: "/var/www/eduardoos.com/html/"
          ARGS: "-rltgoDzvO --delete"